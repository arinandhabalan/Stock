import React from "react";
import ReactDOM from "react-dom/client";
import TradingViewWidget from "./App";
const Root = ReactDOM.createRoot(document.getElementById("root"));
Root.render(<TradingViewWidget />)